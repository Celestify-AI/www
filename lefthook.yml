pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm run lint
    format:
      run: pnpm run format
    check-types:
      run: pnpm run check-types

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit $1
